{
  "name": "praveen-portfolio",
  "version": "1.0.0",
  "description": "Praveen Antony — Full-Stack AI Developer Portfolio (Cloudflare Workers + Pages)",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "dev": "node local-server.js",
    "dev:pages": "wrangler pages dev public/",
    "deploy": "wrangler pages deploy public/",
    "deploy:worker": "wrangler deploy src/worker.js"
  },
  "devDependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.2",
    "wrangler": "^4.81.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
