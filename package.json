{
  "name": "praveen-portfolio",
  "version": "1.0.0",
  "description": "Praveen Antony — Full-Stack AI Developer Portfolio (Cloudflare Workers + Pages)",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy public/",
    "pages:build": "echo 'Pages deployment configured in Cloudflare Dashboard'",
    "pages:dev": "wrangler pages dev public/"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
